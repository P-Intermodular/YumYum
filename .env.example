# Solo incluir variables publicas de desarrollo.
# Nunca subir aqui service_role keys ni credenciales privadas.
SUPABASE_URL=https://lqlekjlatkxurnnwtjgo.supabase.co
SUPABASE_ANON_KEY=pon_aqui_tu_anon_key
APP_BASE_URL=http://localhost:3000

# GEMINI_API_KEY no se configura aqui: vive como secret en Supabase
# (Dashboard -> Project Settings -> Edge Functions -> Secrets) y solo la
# usa la Edge Function `asistente-ia`. Nunca se expone al cliente Flutter.
